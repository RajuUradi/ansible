- name: demo on filters
  hosts: local
  connection: local
  vars:
   topics: linux,shell,console,terraform
  tasks:
   - name: convert string to list
     ansible.builtin.debug:
      msg: "{{topics | split(',')}}" 