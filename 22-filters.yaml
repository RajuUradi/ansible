- name: demo on filters
  hosts: local
  connection: local
  vars:
   topics: linux,shell,console,terraform #string 
  tasks:
   - name: convert string to list
     ansible.builtin.debug:
      msg: "{{topics | split(',')}}"  # using comma to split the string , you can use space also

      