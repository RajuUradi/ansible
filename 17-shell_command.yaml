- name: shell module
  hosts: local
  connection: local

  tasks:
  - name: shell module
    ansible.builtin.shell: echo $HOME 
    register: output

  - name: print
    ansible.builtin.debug: 
     msg: "{{output.stdout}}  "