- name: passing variables as arguments
  hosts: local
  connection: local

  tasks: 
  - name: print argument variables
    ansible.builtin.debug: 
     msg: "{{Name}} is a good human being "

  #  $ ansible-playbook -i inventory.ini -e Name=Raju 09-variables.yaml

  
   