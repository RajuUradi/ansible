- name: loops in ansible
  hosts: local
  connection: local
  become: yes
  
  tasks:
  - name: install packages
    ansible.builtin.packages:
    - name: "{{item}}"
      state: present
    loop:
     - nginx
     - mysql
     - zip
