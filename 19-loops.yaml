- name: loops in ansible
  hosts: local
  connection: local
  become: yes

  tasks:
  - name: install packages
    ansible.builtin.package:
     name: "{{item}}"
     state: present
    loop: # list of strings
     - nginx
     - mysql
     - zip
