- name: loops in ansible
  hosts: local
  connection: local

  tasks:
  - name: print names using loops
    ansible.builtin.debug:
     msg: " {{item}} "

    loop:
    - Raju
    - Jhansi
    - Kishore
    - Chari